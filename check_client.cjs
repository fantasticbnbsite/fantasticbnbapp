const { DatabaseSync } = require('node:sqlite');
const db = new DatabaseSync('/Users/vinijacob/.gemini/antigravity/fantastic-bnb.sqlite'); // wait, the db is local for testing, but I can't check THEIR live db.
